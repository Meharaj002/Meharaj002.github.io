# --- Published repo = hosting essentials only ---
# CV source & working materials are kept LOCALLY but never published.
# The built résumé (cv.pdf, at repo root) IS published and linked by the site.
cv/

# Local tooling / editor config
.claude/
.vscode/

# OS / editor cruft
Thumbs.db
.DS_Store
desktop.ini
*~
